spring:
  datasource:
    driver-class-name:  com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sunglowsys_db
    username: root
    password: root

  jpa:
    show_sql: true

    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect

